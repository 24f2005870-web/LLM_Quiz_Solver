# LLM_Quiz_Solver
This project implements a secure FastAPI-based backend that automatically validates and submits quiz results for the IITM TDS LLM Prompt Testing Assignment. It integrates with Playwright for browser automation, includes secret-based authentication, and exposes a single /api/quiz endpoint for cloud deployment.
